"""Default preset and limit values."""

from __future__ import annotations

DEFAULT_PRESET = "low_poly"
DEFAULT_TARGET = "godot"

DEFAULT_LIMITS = {
    "tri_limit": 120,
    "tex_max": 512,
    "material_limit": 4,
    "bone_limit": 60,
}
